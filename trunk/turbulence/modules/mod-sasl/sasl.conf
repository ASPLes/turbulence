<mod-sasl>
    <!-- db users -->
    <auth-db type="xml" location="auth-db.xml" />

    <!-- allowed sasl profiles -->
    <method-allowed>
      <method value="plain" />
    </method-allowed>
</mod-sasl>